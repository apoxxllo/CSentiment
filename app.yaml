runtime: python
env: flex
entrypoint: gunicorn -b :$PORT app:app -t 600

runtime_config:
    operating_system: "ubuntu22"
    runtime_version: "3.12"

handlers:
- url: /static
  static_dir: static
- url: /.*
  script: app.app

resources:
  memory_gb: 4